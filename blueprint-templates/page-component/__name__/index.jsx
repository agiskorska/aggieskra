import styles from './index.module.css'

import {Button} from '../../common'

function {{name}}() {
  return (
    <div>{{name}}
      <Button text='{{name}}' />
    </div>
  )
}

export default {{name}}